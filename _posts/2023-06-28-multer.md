---
layout: post
title:  "multer 싱글과 멀티"
---
multer는 이미지를 넣게 해주는 편리한 패키지이다
싱글과 멀티 즉 하나의 이미지와 여러개의 이미지를 넣을 수 있다.
싱글에는 성공을 했지만 멀티 이미지는 고전중이다.
고려해야할 부분이 많다. 하지만 싱글로 넣기에는 어려움이 없다.
```
const multer = require('multer');

const storage = multer.diskStorage({
  destination: function (req, file, cb) {
    cb(null, 'views/uploads');
  },
  filename: function (req, file, cb) {
    const uniqueSuffix = Date.now() + '-' + Math.round(Math.random() * 1E9);
    cb(null, uniqueSuffix + path.extname(file.originalname));
  }
});

const upload = multer({ storage: storage }); //사진 크기를 제한하고 싶다면 limit를 사용하면 가능하다.
//이 아래 코드는 사용하는 html과 name과 db의 이름을 넣어 작성해야 한다.
```
html과 이름이 맞게 하고 db와 이름이 같다면 사진이 잘 들가는걸 확인할 수 있다.
