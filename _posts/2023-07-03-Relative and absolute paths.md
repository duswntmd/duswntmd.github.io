상대 경로
```
C:\users\wn\path.js
```
상대경로는 현재 파일을 기준으로 한다. 
현재 파일과 같은 결로면 (.)를 현재 파일보다 한 단계 상위 경로면 점 두개(..)를 사용한다.

절대 경로
```
C:\
```
절대경로는 루트 폴더(C:\)나 노드 프로세스가 실해되는 위치가 기준이다.

C:\users\wn\path.js에서 C:\로 가고 싶다면 절대경로에서는 그냥 C:\를 입력하면 된다.
하지만 상대경로 에서는 ..\..을해야 두 디렉터리 위로 올라가 C:\가 된다.
